{
  "name": "@ats-express/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos y enums compartidos entre backend y frontends — ATS-Express",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
